{
  "name": "selah-app",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node index",
    "build": "cd client && ember build && cd ../viewer && ember build",
    "test": "cd client && ember test && cd ../viewer && ember test"
  },
  "repository": "https://github.com/stefanpenner/ember-cli",
  "engines": {
    "node": ">= 0.10.0"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cookie-parser": "^1.3.3",
    "express": "^4.9.0",
    "express-handlebars": "^1.1.0",
    "express-session": "^1.8.2",
    "morgan": "^1.3.0",
    "socket.io": "^1.1.0",
    "socket.io-client": "^1.1.0"
  },
  "devDependencies": {
  }
}
