import addUserToSession from 'selah/utils/add-user-to-session';

module('addUserToSession');

// Replace this with your real tests.
test('it works', function() {
  var result = addUserToSession();
  ok(result);
});
